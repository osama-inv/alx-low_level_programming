#include "main.h"
/**
 * _isalpha - def Important.
 * @c : a char
 * Return: Always 0 (Success)
 */

int _isalpha(int c)
{
if (c <= 'z' ‘&&’ c >= 'a' ‘||’ c <= 'z' ‘&&’ c >= 'a')
return (1);
return (0);
}

